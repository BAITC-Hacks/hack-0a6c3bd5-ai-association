# Стартовый запрос для Claude

Скопируйте текст ниже в Claude, работающий с отдельным клоном этого репозитория. Передайте файлы явно или попросите прочитать их из доступной рабочей папки; этот документ не предполагает автоматическую загрузку контекста каким-либо приложением.

```text
Помоги упростить UX существующего «Контура» — консультанта по закупке электротоваров ekt.kz. Пользовательская проблема: «весь путь непонятен, с чего начать». H1–H3 уже работают, переписывать проект заново нельзя.

Сначала прочитай AGENTS.md, docs/CLAUDE-HANDOFF.md, docs/API-CONTRACT.md, README.md и docs/DEMO.md. Изучи текущий интерфейс и код web/src/. Дай краткий план: что изменишь, какие файлы затронешь, как сократишь лишние переходы и как проверишь. По AGENTS.md дождись моего «го» до правок.

Предложи простой путь от запроса или файла к проверке и подтверждённой корзине. Рекомендации в handoff — отправная точка, а не обязательный макет. Сохрани все рабочие возможности, черновик строк при навигации, реальные API, серверные деньги и остатки, null≠0, источники и явное обозначение учебных данных.

Не ломай useCommerce/api: подтверждение относится к конкретному показанному proposal ID своей сессии и склада; изменение/удаление требуют подтверждения; история не даёт старых CTA. Сохрани проверки срока/контекста, UUID при повторе, защиту от двойных действий и обработку ошибок. Корзина остаётся в SQLite, UI-моки и localStorage-корзина запрещены. Любые правки файла/строк/склада снимают старое предложение.

Работай в отдельной frontend-ветке и клоне, не мешай текущей H4-проверке команды. Основная зона — web/src/ и пересобранный web/dist. Backend app/, data/, fixtures/, Python-зависимости и запуск не меняй. Общие документы согласуй с Мовсаром.

Сохрани React + TypeScript + Vite + Tailwind, собственный CSS, локальные шрифты/иконки. Без готовых UI-китов, CDN, лишних зависимостей и кнопок-пустышек. Не меняй байты учебных файлов examples и лицензии.

После согласования плана делай небольшие рабочие итерации. Проверь сборку npm run build, сценарий DEMO.md через браузер, desktop/390 px, отмену и подтверждение, смену склада, исправление файла, устаревший ID, повтор после потери ответа и сохранение корзины после reload. Итог проверяется через uv run hack serve с закоммиченным web/dist без ключей и внешних запросов. Покажи скриншоты и честно перечисли выполненные проверки и ограничения.
```
