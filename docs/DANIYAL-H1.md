# Стартовый промт Даниялу — H1

Скопируй текст ниже в свою сессию Codex, открытую из клона репозитория.

> Я Даниял, участник A, Windows 11. Прочитай AGENTS.md, docs/TEAM.md, docs/SPEC.md и docs/API-CONTRACT.md. Делаем «Контур» для официального кейса ekt.kz. Моя зона: app/, data/, fixtures/; для запуска H1 также pyproject.toml, uv.lock, requirements.txt, Dockerfile, compose.yaml и .env.example. Мовсар параллельно делает web/, docs/ и README.md — не редактируй их. Сначала покажи краткий план H1 и дождись моего «го». Затем реализуй ТОЛЬКО H1 из контракта: health, warehouses, поиск/список товаров, карточка с наличием по выбранному складу; Python 3.12, FastAPI, SQLite, uv. Сделай hack seed и hack serve. DEMO_MODE=1 по умолчанию, сид и запуск без сети и ключей, ошибки по контракту. В H1 LLM не нужен. API ekt.kz подключай через адаптер чтения; credentials — только локальные env, не в код и Git. Существующая сборка web/dist должна отдаваться FastAPI; если её пока нет, API должен запускаться самостоятельно. Не меняй согласованный JSON без согласования. Проверь seed, запуск, endpoint, пустой поиск и несуществующий товар. Коммить только свои конкретные файлы; перед push — git pull --rebase при чистом рабочем дереве. Пришли Мовсару хэш коммита, команду запуска, фактический JSON и ограничения. H1 не считай завершённым до проверки общего UI.

Перед стартом выполни `git pull --rebase origin main` при чистом рабочем дереве. Если контракт расходится с текущим кодом, сначала согласуй это с Мовсаром. Доступы ekt.kz бери из предоставленной организаторами инструкции; секреты в чат и репозиторий не вставляй.
